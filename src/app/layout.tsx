import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Fundsphere - Future Strategic Finance for Entrepreneurs',
  description: 'Scale with checking and savings accounts, custom tools, and access to our investor network.',
  keywords: 'finance, fintech, entrepreneur, funding, strategic finance',
  authors: [{ name: 'Fundsphere' }],
}

export const viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
} 